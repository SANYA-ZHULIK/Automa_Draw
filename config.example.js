// === КОНФИГУРАЦИЯ SUPABASE (ОБРАЗЕЦ) ===
// Скопируйте этот файл в config.js и заполните свои значения

const CONFIG = {
    // Ваш Supabase URL (найдите в Settings → API)
    SUPABASE_URL: 'https://your-project.supabase.co',
    
    // Ваш публичный ключ (anon key) из Settings → API
    // НЕ используйте service_role ключ в frontend!
    SUPABASE_KEY: 'your-anon-key-here'
};
